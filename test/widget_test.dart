// Default placeholder test replaced. Real tests live alongside the units
// they cover under test/core/ and test/features/.
//
// This file remains so `flutter test` discovers _at least one_ test when
// the project is freshly cloned without any feature tests in place.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder — see test/core and test/features for real tests', () {
    expect(1 + 1, 2);
  });
}
