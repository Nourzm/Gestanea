name: flutter_inset_box_shadow
description: This package extends BoxShadow and BoxDecoration to support the inset property.
version: 1.0.8
repository: https://github.com/qoumo/flutter_inset_box_shadow

dependencies:
  flutter:
    sdk: flutter

# Patched: bumped upper SDK bound from <3.0.0 to <4.0.0 for Dart 3.x compatibility.
# No source changes — this is the only difference from the original pub.dev package.
environment:
  sdk: '>=2.12.0 <4.0.0'
